#include <iostream>

using namespace std;

int main() {

  char* in;
  
  cout << "Hello, user. Please enter in your equation, using infix format, separated by spaces (e.x. 3 + 4): ";

  cin.getline(in);

  cout << strlen(in); << endl;
  
}
