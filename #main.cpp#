#include <iostream>
#include <cstring>

using namespace std;

int main() {

  char* in = new char[1000];
  
  cout << "Hello, user. Please enter in your equation, using infix format, separated by spaces (e.x. 3 + 4): ";

  //Get original infix format function from user
  cin.getline(in, 1000);
  cout << endl;
  
  //Parse that thing into individual statements, and separate operators from values
  for (int i = 0; i < strlen(in) - 1; i++) {
    
  }
  
}
